Remove the simulator type option in settings. Instead constantly attempt to connect to each. There is an assumption that users will only run one simulator at a time on a single machine.

In flight metrics don't display the big table with all the details if there is no ongoing flight.

